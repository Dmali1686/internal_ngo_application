import 'package:flutter/material.dart';

class AmbulanceDashboardScreen extends StatelessWidget {
  const AmbulanceDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AmbulanceDashboardScreen')),
      body: const Center(child: Text('AmbulanceDashboardScreen Content')),
    );
  }
}
