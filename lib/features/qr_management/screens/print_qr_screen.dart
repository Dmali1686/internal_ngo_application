import 'package:flutter/material.dart';

class PrintQrScreen extends StatelessWidget {
  const PrintQrScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PrintQrScreen')),
      body: const Center(child: Text('PrintQrScreen Content')),
    );
  }
}
