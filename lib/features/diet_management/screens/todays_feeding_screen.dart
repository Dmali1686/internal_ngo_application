import 'package:flutter/material.dart';

class TodaysFeedingScreen extends StatelessWidget {
  const TodaysFeedingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TodaysFeedingScreen')),
      body: const Center(child: Text('TodaysFeedingScreen Content')),
    );
  }
}
