import 'package:flutter/material.dart';

class DietHistoryScreen extends StatelessWidget {
  const DietHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DietHistoryScreen')),
      body: const Center(child: Text('DietHistoryScreen Content')),
    );
  }
}
