import 'package:flutter/material.dart';

class VoiceNotesDashboardScreen extends StatelessWidget {
  const VoiceNotesDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('VoiceNotesDashboardScreen')),
      body: const Center(child: Text('VoiceNotesDashboardScreen Content')),
    );
  }
}
