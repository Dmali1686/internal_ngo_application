import 'package:flutter/material.dart';

class PlaybackVoiceNoteScreen extends StatelessWidget {
  const PlaybackVoiceNoteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PlaybackVoiceNoteScreen')),
      body: const Center(child: Text('PlaybackVoiceNoteScreen Content')),
    );
  }
}
