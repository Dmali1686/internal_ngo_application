import 'package:flutter/material.dart';

class RecordVoiceNoteScreen extends StatelessWidget {
  const RecordVoiceNoteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RecordVoiceNoteScreen')),
      body: const Center(child: Text('RecordVoiceNoteScreen Content')),
    );
  }
}
