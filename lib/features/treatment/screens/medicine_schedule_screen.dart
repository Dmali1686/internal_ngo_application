import 'package:flutter/material.dart';

class MedicineScheduleScreen extends StatelessWidget {
  const MedicineScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MedicineScheduleScreen')),
      body: const Center(child: Text('MedicineScheduleScreen Content')),
    );
  }
}
